public class concat {
    public static void main(String[] args) {
        String first_name = "First";
        String last_name = "Name";

        System.out.println(first_name.concat(" " + last_name));

        // Concat method can be used to attach to strings
    }
}
