public class toUpperCase {
    public static void main(String[] args) {
        String name = "Name";

        System.out.println(name.toUpperCase());

        // toUpperCase method can be used to return a string only in Capital letters
    }
}
